;; Isolate package configuration in a way that is performance-oriented and tidy
(require 'ert) ;; https://github.com/jwiegley/use-package/issues/85
(require 'use-package)

(provide 'setup-configuration-tools)
